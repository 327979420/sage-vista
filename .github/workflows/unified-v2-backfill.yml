name: Unified V2 Historical Backfill

on:
  workflow_dispatch:
    inputs:
      start:
        description: First trading date
        required: true
        default: "2026-07-01"
      end:
        description: Last trading date
        required: true
        default: "2026-08-27"

permissions:
  contents: write
  actions: write

concurrency:
  group: sage-vista-v2-backfill
  cancel-in-progress: false

jobs:
  partitions:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.weeks.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.13" }
      - name: Turn requested dates into recoverable weekly checkpoints
        id: weeks
        run: python3 -m services.scanner.date_partitions --start "${{ inputs.start }}" --end "${{ inputs.end }}" --github-output "$GITHUB_OUTPUT"

  backfill:
    needs: partitions
    strategy:
      fail-fast: false
      matrix:
        partition: ${{ fromJSON(needs.partitions.outputs.matrix) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-python@v5
        with: { python-version: "3.13" }
      - name: Restore audited historical price cache
        uses: actions/cache@v4
        with:
          path: |
            work/eodhd-cache
            work/eodhd-active-common.json
          key: eodhd-history-v1-${{ github.run_id }}
          restore-keys: eodhd-history-v1-
      - name: Rebuild requested history without future classifications
        run: python3 -m services.scanner.unified_v2_scan --start "${{ matrix.partition.start }}" --end "${{ matrix.partition.end }}" --out "work/v2-${{ matrix.partition.label }}.json" --replace
      - name: Validate scanner and repository
        run: python3 -m unittest discover -s tests -p 'test_*.py'
      - name: Save completed week before aggregation
        uses: actions/upload-artifact@v4
        with:
          name: unified-v2-${{ matrix.partition.label }}
          path: work/v2-${{ matrix.partition.label }}.json
          retention-days: 30

  aggregate:
    needs: backfill
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-python@v5
        with: { python-version: "3.13" }
      - name: Restore historical EOD cache for elapsed outcomes
        uses: actions/cache@v4
        with:
          path: |
            work/eodhd-cache
            work/eodhd-active-common.json
          key: eodhd-history-v1-${{ github.run_id }}
          restore-keys: eodhd-history-v1-
      - uses: actions/download-artifact@v4
        with:
          pattern: unified-v2-*
          path: work/v2-parts
          merge-multiple: true
      - name: Merge saved months with any newer published day
        run: python3 -m services.scanner.merge_unified_v2_reports public/unified-v2-rankings.json work/v2-parts/*.json --out public/unified-v2-rankings.json
      - name: Preserve every merged signal and calculate elapsed outcomes
        run: python3 -m services.scanner.opportunity_ledger
      - name: Validate merged historical output
        run: python3 -m unittest discover -s tests -p 'test_*.py'
      - name: Commit historical research output
        run: |
          git config user.name "sage-vista-bot"
          git config user.email "sage-vista-bot@users.noreply.github.com"
          git add public/unified-v2-rankings.json public/opportunity-ledger.json
          git diff --cached --quiet && exit 0
          git commit -m "data: unified V2 backfill ${{ inputs.start }} to ${{ inputs.end }}"
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Hand off publication to the website deploy workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run deploy-site.yml --ref main
