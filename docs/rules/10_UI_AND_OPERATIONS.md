# 10｜网站、自动化与生产交付规则

版本：`1.17.29`
最后更新：2026-09-07

## 本文件负责

网站每页要解决的问题、实用表达、版本、每日更新、部署、线上核验和通知。

## 网站宗旨

网站是项目最终可用成品，不是工程日志。每个主要页面必须直接回答：**这是什么、数据更新到哪天、分数怎么来、我现在怎么用、有什么限制。**

## 页面职责

| 页面 | 首要问题 | 必须显示 |
|---|---|---|
| 今日总览 | 今天先看什么 | 大盘置顶、当天精选、关键原因、风险、数据日期 |

## 复杂多因子与简化形态的页面分工

- 多因子机会页是复杂版：榜首先显示“正向证据几颗、覆盖几个家族、哪些是父子重复确认、哪些家族发生跨周期共振”，再显示每一颗的周期、家族和研究状态。
- 旧审计的0分／不稳定因子只要今天客观命中，仍必须出现在颗数和明细中；页面同时标明“被计为技术证据”与“尚未验证收益”是两回事。
- 行业和大盘使用独立列和颜色，只作为技术完全同分后的上下文，不再把`技术＋行业＋大盘`显示成一个似乎可比较的总分。
- “我最喜欢形态”页是简化版：首屏固定四项`回调、双底、三推突破、Golden Pocket／EMA承接`，显示`4/4`或缺哪一项；旧V2七阶段移到历史对照字段，不再主导页面。
- 两页都必须保留风险与为什么未升级、版本、数据日和具体点时证据；颗数与4/4均不得标成胜率、预期收益或自动买入建议。
| 多因子机会 | 排名为何如此、个股为什么值得看 | 唯一排行榜、技术 / 行业 / 大盘分项、精选子集、个股日周月详情、主导周期、冲突与风险计划 |
| 我最喜欢形态 | 今天哪些股票完成或接近个人最佳形态 | 四项完成度、近似机会与明确缺项、图形与关键位、机制教学案例、风险反例、前向样本 |
| 行业与大盘 | 市场和热点如何 | 趋势、广度、风险偏好、ETF 热点、个股关联 |

正式网站只保留以上四个入口。实验账本、预登记、负结果、详细回测和策略证据保留在Git后台；不再把完整实验目录、旧实验详情页、回测断点或逐实验JSON作为公开网站产品。

早期EODHD验证、市场环境和中性化研究结果统一保存在`research/backtest/output/legacy-foundation/`，不得继续放在`public/`随网站发布。移动只改变存放位置，不改变实验内容。

## 实用性规则

- 结论在前，工程细节折叠；失败和限制不能隐藏。
- 过期候选必须标日期与状态，不得冒充今日推荐。
- 每一分可追溯；不能只写“高分”而没有命中与未命中项。
- UI 版本固定显示在顶部；数据、模型、因子与策略版本可核对。
- 桌面和移动端工具栏对齐，正文可读，功能区分明显。
- 实验目录、工程 ID、英文原始结果、完整限制和配置保留在 Git 账本与中文实验摘要；正式网站不再复制一套实验中心。
- 行业与大盘必须在同一主要页面先显示今日大盘结论、趋势 / 广度 / 风险偏好，再显示领先行业、回调观察、修复行业和数据不足；表格是证据，不是首页答案。
- 多因子机会页的因子库必须在排行榜前用四象限回答：`正在使用 / 候选观察 / 暂停加权 / 准备弃用`。象限表示当前处理动作，卡片还必须单独显示生产角色和最新研究结论；“正在使用”不得冒充“已验证有效”。
- 同一因子家族在四个象限中使用同一稳定颜色，并同时显示家族文字，不能只靠颜色传达含义。象限状态使用独立标题、边框和动作文字，不能与家族色混淆。
- 四象限和“现在能直接用的结论”必须读取最近完成且已审计的实验摘要；不得继续以短期机会账本或旧实验摘要覆盖更新的全期研究。
- 因子被标为“准备弃用”时仍保留机器定义、历史命中和失败实验；这里表示从主研究视图与未来评分候选中退休，未经迁移批准不得删除历史证据。
- 多因子机会页只保留当前决策链：今日摘要、最新审计结论、四象限因子库、唯一排行榜、选中股票的周期与风险解释、永久机会账本。旧系统历史排行、旧因子库、独立股票触发查询、旧评分兼容观察和旧规则说明不得继续在该页展示或加载。
- 从多因子机会页移除旧模块只表示退出产品主界面，不删除历史JSON、实验产物或失败记录；需要复核实验时统一读取GitHub留档。
- 后续图表版必须等待相应回测数据口径冻结；收益曲线、分组条形图和样本构成图不得把观察分包装成收益概率。
- 因子家族组合实验的完整15组合、年度明细与匹配对照只保存在Git；多因子页只吸收经过批准的因子角色或动作结论，不重复展示整组实验数字。
- “我最喜欢形态”必须是一个独立、单一目的页面，不恢复旧MACD Tracker的全部排名、查询、旧评分或重复因子库。首屏用阶段漏斗和候选数量回答“现在走到哪一步”，候选详情用价格／EMA曲线、两底、三推趋势线、突破位与前高目标解释机器判断。
- 页面固定显示“4/4不是胜率”；达到最低前向样本门槛前，只展示样本积累和可人工复查案例，不展示暗示有效性的胜率结论。
- V3页面只显示回调、双底、三推收盘突破、Golden Pocket／EMA承接四项；风险闸门必须与正面进度并列，不得藏在折叠区。旧V1/V2阶段只保留为只读对照。
- 页面必须直说案例负责人工校验而非证明收益。ADBE／BABA用于教学，TTD／AEVA用于风险边界；PG只允许在V1历史审计中出现。
- 增加“接近但未完成”观察区：每只股票显示四项完成数、已完成机制、关键缺口和风险阻断。无风险近似与已阻断近似视觉分开，并固定提示“不是买入信号、不进入收益统计”。
- 正式候选和近似观察必须读取同一日点时数据，但近似观察不得写入永久交易账本、Discord通知或唯一生产排行。
- 每个正式页面只能下载自身必需的生产数据。共享页面外壳只读取轻量更新状态，不得让今日、多因子和行业页为了显示日期而下载完整旧Tracker。
- 今日页只读取最新排行榜和追踪摘要；多因子页先读取最新榜与最近账本事件，只有用户主动复盘旧日期时才加载完整排行榜和永久账本。
- “我最喜欢形态”使用独立精简公开数据，只包含正式候选、近似候选、参考案例、四项证据、V1/V2只读摘要和汇总；完整全市场候选仍留在内部Tracker与Git审计，不在浏览器重复下载。
- 多因子机会保留完整四象限因子库、唯一排行、个股证据、风险计划和机会追踪；已完成实验的完整数字、组合明细及旧研究页面退出前台，避免同一负结果在多个区块重复展示。
- 行业与大盘首屏固定优先显示SPY、QQQ、IWM、RSP及有可靠来源的常用行业ETF代码（例如SOXX）；详细数据质量和成员审计折叠保存，不让日常判断被研究工程字段淹没。
- 组合卡必须明确区分`历史收益冠军`、`通过滚动门槛的零权重候选`和`生产正在使用`；历史冠军不得以绿色通过态或“最佳买入策略”表达。实验处于预登记或运行中时，网站只显示进行中，不提前展示临时赢家。

## 自动更新和发布

权威顺序：生成数据 → 数据审计 → 测试 → 保存版本 → 构建 → Cloudflare 部署 → 线上核验 → Discord。

- `main` 上的 UI 或生产页面依赖发生变化时，网站发布工作流自动构建、测试和上线；纯实验定义、实验目录与后台回测结果只更新Git档案，不再单独触发网站发布。
- 每日 EOD 工作流使用 `main` 上当前已批准的生产规则，原子生成大盘、行业、个股、排行榜和永久跟踪数据，然后测试、发布并核对线上日期。
- 时间敏感的每日 EOD 不得只依赖 GitHub `schedule`。Cloudflare Cron 是独立主触发源，使用限定到本仓库 Actions 写权限的专用令牌 dispatch 同一条日终工作流；GitHub schedule 只作备用。两者都必须复用同一幂等入口，不能另算一套行情。
- 独立新鲜度检查同样由 Cloudflare Cron 触发。若数据源已有更新而网站仍落后，检查任务必须自动 dispatch 一次 `freshness_recovery` 日终更新；恢复任务仍走完整的数据审计、测试、部署、线上核验和通知闸门，不能绕过任何一步。
- 日终运行必须记录 `cloudflare_cron / github_schedule / freshness_recovery / manual` 触发来源。重复触发在确认全套数据与当前因子版本一致后以 `already_current` 安静退出。
- Cloudflare 调度器不得复用个人广权限登录令牌。专用令牌只允许访问本仓库并具有触发 Actions 所需的最小权限；只能作为 Cloudflare secret 保存，禁止进入 Git、普通变量、日志或公开产物。
- Cloudflare 只配置两条 Cron Trigger：一条覆盖日终候选窗口，并由 Worker 内部 UTC 白名单保留原有7个精确启动时点；另一条独立触发新鲜度检查。非白名单唤醒必须立即空退出，不调用 GitHub，避免超过免费账户5条 Cron Trigger 的额度，也不能借合并改变实际生产时间。
- 没有新完整交易日时保留上一交易日的有效数据，不伪造“今天”数据；代码和 UI 更新仍走独立自动发布通道。
- 若规则在没有新完整交易日时升级，网站可用上一完整日做新版校准展示，但不得把该日写成新版前向首发事件，也不得覆盖同日旧模型历史排行；真正前向从部署后的下一完整交易日开始。
- 实验规则可以持续新增，但在回测通过并更新规则手册前，不得因一次代码合并自动替换生产评分。
- 任何关键数据错位、测试、构建或线上核验失败都停止通知。
- Discord 读取已发布的同源结果，不另算一套分；同股票、日期、状态去重。
- 每日生产与多年回测隔离，回测失败不能拖死当天更新。
- 夜间回测状态保存在Git机器状态与中文当前状态文档；不再要求公开网站承载实验进度。
- 夜间回测每次只处理一个自然周并先保存周产物；失败不移动断点，下一晚重试同一周。新增规则只影响后续新批次，旧批次保留原版本。
- 多周批量回放仍必须把每个自然周作为独立可恢复产物；只有全部周成功后才汇总、提交和发布。
- 完整研究周产物与网站视图分开：网站保留最近30个交易日的完整因子审计；更早日期保留排名、分数、原因及已命中因子，省略大量重复的“未命中”明细，以符合网站单文25MiB限制。
- 2000—2025 长期回放不逐年合并进网站数据：每个自然周明细和年度压缩包保存在 GitHub Actions；整个时期完成后，网站只发布总样本、五年分段、2025独立验证、2026前向观察、因子层级和退出对照，不发布逐笔明细。
- Secrets 只用于运行环境，报错不得打印带 Token 的完整网址。
- 向用户交付时提供生产链接；未部署要明确说“代码完成但生产未更新”。

## M12已批准实施边界（未上线）

- CR-053按`../M12_PRODUCTION_CHAIN_MINIMAL_DESIGN_ZH.md` v0.2.1分包实施；发布编排唯一负责人为services/publication，各合同仍统一经services/contracts/validation.py验证。
- 先合同与持久存储，后原信号跨日M08／M09续跑、独立M10评价、四页投影和发布回退；原信号掉榜仍续跑，已存事件和政策不重写。
- SourceInventory只能由可信库存锁内的完整根和可达引用构造；缺失解析证据、删增替换、循环或内容冲突失败关闭，声明列表不能自证完整。
- 正式存储适配不得取消原shadow路径守门。当前首批仅纯合同基础，不创建云资源或接入工作流；代码通过不代表持久存储或跨日端到端已经通过。
- 当前研究准备授权只能从受信daily锁下的完整授权索引／原件解析得到；不可挑旧grant绕过当前revoke或其他配置的新链头。当前链头必须为匹配实际配置原字节语义指纹、运行代码且在目标日及检查时纽约日期均有效的grant。配置政策全集、Git来源、实际许可与使用前重查仍需可信工厂完成，纯检查返回的引用不是可复用生产通行证。
- 首轮运行配置保存v0.2.1完整既有政策及源身份，业务定义固定14fef535，运行代码使用明确完整提交号；配置生产者只从仓库固定Git对象取证，业务源漂移失败，不能只比较版本字符串。M12新增适配代码以独立运行提交绑定，不冒充旧业务定义；配置内容和来源通过唯一合同入口校验，配置产生本身不授予运行或数据使用权限。
- 首轮research_only、策略激活为空；上线日期、部署、生产启用和真实通知仍等待上线卡明确批准。用户后续已授权工作分支普通同步，阶段审核后由审核任务推送已审SHA；main合并与上线仍须单独授权。

- C2a允许gates/baseline.py无行为变化谓词提取的唯一精确运行blob替代项；113份14fef535定义源清单不删减，其他运行源仍逐字节冻结。实际替代项由唯一配置验证器重算并写入runtime_source_overrides，旧运行源不新增该字段；任意其他源码漂移拒绝。该记录不是策略变更或运行授权。

- C2c成功成员观察使用单个持久顺序根，跨日／配置提交仍比较完整旧根；日期只能前进，同日同观察在读回当前根后幂等，不同观察冲突停止。daily租约及原身份期限内成对提交索引／日志／根；缺件不自动重建，空库仅可信迁移显式初始化。此内部存储能力只接上层已经验证并读回的成功原件，不提供RPC、不自证来源成功或数据授权。

- C2d成员登记固定字节输入须绑定原准备身份、当前完整源索引、候选观察原件和固定许可引用；唯一Python入口联合原授权／Git配置及M02来源身份检查，计算完成再次核时间窗。原固定进程仅增加封闭成员协议分支，结果绑定原输入和登记摘要，不表示服务端已登记；上限内保留全部历史，超限拒绝，不能裁剪来源冒充完整。

- C2g每日认证通道仅开放固定成员登记prepare／return协议，服务器自行保存实际输入并绑定回传；响应编码后仍须读回原记录／原件且通过全事务原日期期限。客户端只能运行固定worker并核输入、当前任务与实际返回摘要／登记根，失败或不确定回执不得标成登记成功或自动重试。daily runner仍禁用，登记成功也不是formal行情资格或发布授权。

- C2f原准备证据的纽约日期截止必须与身份、许可及租约一起约束整个事务完成；prepare最后复制、首次登记与幂等回传都不能跨原日期返回成功。日期截止复用原纽约时区判断，不以固定UTC小时或JWT期限代替。

- C2f登记必须持久保存服务器实际输入与配对日志；固定结果须绑定输入sha／长度／开始完成窗口、原准备摘要及候选／expected_index。接受前读回全部实际原件并重查当前许可、准备选择、授权和原身份／租约；返回配对、登记回执及索引追加一笔同步事务完成。原任务重试只在回执配对完整、实际原件仍可核且根等于原完成根时返回，缺件／变根／到期不自动恢复。不得把内部会话暴露成任意索引写入口。

- C2e成员登记输入由服务器从已核验准备选择、固定许可和持久完整成功观察根自行读回构造；未登记候选必须属于当前采集会话，历史读取仅沿已登记根可达raw引用。异步后／编码后复查当前根、选择、许可及原身份租约，不允许调用方交完整源列表自证权限。只读输入不是持久验证票据或登记回执，实际回传绑定／append保持后续接点。

- C2h日链只组合M02原3.x准备及M03—M09唯一入口，保留原始不可变批次，禁止覆盖检测器或九项政策。D日计划统一保留原M08缺次日行情决策；缺必需M02快照失败关闭，不生成假的空排行。该纯计算接点不验证实际供应商许可、不自证源完整、不登记库存、不接启用入口。

- D3a续跑只读原排行／支撑／事件，按显式已核交易会话补原入场日计划、逐日调用原M08退出并追加M09关联；掉榜不取消。已存计划及状态先核身份并复用，恢复先补原关联再推进，缺日只推进连续前缀，已观察前缀改动或分叉失败。纯输出顺序不等于DO原子登记或持久断点；真实会话来源未齐不得启用。

- D3b内部execution目录显式初始化；任务根与索引日志、追加对与配对日志及头在同一事务写入。读取必须沿持久目录取全历史并核归档原字节；原件或记录缺失不自愈。只有原输入、M08对象和M09关联全部读回且原租约／CAS仍有效才追加成对序号；该存储序号不自证业务语义、来源完整或完成日期。真实接线仍需原Python验证及可信配置工厂。

- D3c成对原件必须通过Python复用原续跑及M08／M09验证重放后才派生业务叶，原输入、对象和关联字节共同绑定。冻结入场读数从完整持久历史恢复，重复语义沿用已存原件，不换generated_at。执行包仍有M02／配置外部引用，不能独自生成或冒称完整SourceInventory；正式库存必须经既有唯一合同入口闭合这些引用。

- D3d任务级库存必须由已登记源包及全执行历史读回构造；源包固定配置原字节、完整M02输入，原日链重放必须与冻结信号一致，配置ETF集合和政策不变。依赖图由已解析原件推导，不接受调用者提供roots/nodes。无源包旧执行根只能原范围恢复，不能生成完整SourceInventory；任务库存不等于全日发布库存或来源使用授权。

- D3e固定隔离进程只新增封闭execution协议，仍在导入前及输出前核完整提交源码；本次运行配置必须匹配实际身份代码；历史源配置按下述D3g单独核验。计算结果绑定原输入摘要／长度和完整task snapshot，原身份期限内完成，最大2MiB不裁剪，失败不回成功形状。该计算接点须由服务端持久派发／返回绑定及原租约再次确认，单独结果不是写入许可。

- D3f内部计算会话自行读完整任务、保存实际输入和配对日志，返回只绑定该原输入及任务快照。固定返回原件和下一M08／M09对的登记收据同事务保存，原输入或配对缺失不重建；原身份期限不能由后续请求延长。此工厂要求既有受信身份来源，不新增RPC或赋予调用者认证能力。

- D3g历史源配置保留原提交，本次固定计算另验证当前身份代码的真实Git配置。除code_commit及已批准无行为源码替代记录外，全部配置语义必须一致；旧源不能静默升级，新业务配置不能借续跑越过原政策。原输入字节、源配置Ref及任务根继续冻结，只有兼容运维代码可以续跑。

- D1a任务重试沿用v0.2.1固定预算：每纽约日最多3次，暂时失败后15分钟／60分钟／下一已核EOD，单Job最多10分钟。未成熟仍queued，不领取不耗attempt；身份／合同冲突blocked，不自动恢复。运维状态不能替代M10不可变完成证据，真实日历不得由周末猜测。

- D1b执行领取从原持久任务目录读取，不使用今日排行。Job起点及attempt记录／配对日志／头须持久化；重新打开、续租或换runner不清空原日预算。失联只由新fence持有者记录runner_lost再按原延迟恢复，旧fence不得记录失败或完成。可信成熟／EOD解析器未安装时不可领取，内部调度不自证M10完成。

- D1c本地执行链的可推进状态取固定worker实际结果，不由任务调用方提供mature。checkpoint须重新读回原会话完成回执及全任务原件，分别保存原source_snapshot与登记后snapshot；无下一对只表示本次截止无新推进，不冒充任务或M10终结。运行监督按原身份、任务租约与Job预算取消，真实EOD来源未接时不得解除第三次失败等待。

- D1d预算/取消只停止计算并保存可核checkpoint的queued/yield记录，不当业务completed；既有重试延迟、日预算和blocked必须在启动下一固定计算前检查。身份/租约失效不借记录失败继续写入；固定计算无合法证据保留blocked，存储不确定保留原dispatch，不能猜测成功。

## 当前优先级

先保证技术扫描、评分、追踪、实验结论真实可用，再精进行业、大盘和整体视觉。不能为了美化掩盖数据或策略尚未完成。

旧“个股研究 / MACD Tracker”页面和旧排名仍保持退役。新“我最喜欢形态”是用户批准的单一形态状态机，不恢复旧排名、旧评分、旧查询或重复因子库；通用个股因子审计仍由“多因子机会”承接。

## 联动条件

页面只改变表达时只改本文件；如果改变分数、选择或实验结论，必须回到对应业务模块先改规则。

## 变更记录

### 1.17.29 — 2026-09-07

- D1d将预算yield与运行失败语义接入原本地链，保持失效写入/原件边界。

### 1.17.28 — 2026-09-07

- D1c固定可推进证据、实际会话checkpoint与本地有界监督接线；旧桥有界诊断不改变生产守门。

### 1.17.27 — 2026-09-07

- D1b原执行目录上的持久领取及独立预算，保持无公开RPC与原完成证据边界。

### 1.17.26 — 2026-09-07

- D1a固定重试及Job预算先行，保持成熟度、业务完成与真实日历的原入口职责。

### 1.17.25 — 2026-09-07

- D3g区分冻结源配置和本次运行配置，保留原业务政策并允许已核兼容代码续跑。

### 1.17.24 — 2026-09-07

- D3f实际派发／结果与成对登记绑定，复用原execution事务与失败关闭。

### 1.17.23 — 2026-09-07

- D3e固定执行库存计算协议及有界结果；旧协议上限、源码及取消守门不变。

### 1.17.22 — 2026-09-07

- D3d冻结源包重放与任务级SourceInventory推导，保持旧根和正式调用边界。

### 1.17.21 — 2026-09-07

- D3c持久执行原件重放、入场读数续用及业务叶确认，来源库存外部引用仍失败关闭。

### 1.17.20 — 2026-09-07

- D3b持久任务根、完整历史与原件成对归档读回；保持内部能力和语义验证分工。

### 1.17.19 — 2026-09-07

- D3a原信号冻结、逐日续跑和本地部分写入恢复接线，原M08／M09算法及存储守门不变。

### 1.17.18 — 2026-09-07

- C2h固定日链组合、冻结批次交接及来源／发布边界；原业务算法不变。

### 1.17.17 — 2026-09-07

- C2g接每日认证登记协议、固定Python调用及禁用runner，保留编码后复核和无实际启用边界。

### 1.17.16 — 2026-09-07

- 修C2f独立P2：将原证据纽约日界纳入事务最终deadline，覆盖准备、登记及幂等返回。

### 1.17.15 — 2026-09-07

- C2f绑定实际发送／返回与成功观察CAS，明确部分提交回滚和原任务重试边界，保持未安装公开入口。

### 1.17.14 — 2026-09-07

- C2e补受控成员登记原件读回，区分当前任务候选和已登记历史读取范围；固定输入构造不等于写入授权。

### 1.17.13 — 2026-09-07

- C2d复用固定worker校验成员登记原件与当前准备材料，保留旧协议、源码／取消／时间闸门和服务端实际登记独立边界。

### 1.17.12 — 2026-09-07

- C2c新增成功观察持久顺序根／CAS及成对日志，复用原daily租约，保持缺件失败关闭和无生产接线边界。

### 1.17.11 — 2026-09-07

- 记录用户后续已授权工作分支同步、审核任务已同步ddba447；保留main合并、部署、生产启用及真实通知独立审批。

### 1.17.10 — 2026-09-07

- C2a精确记录资格谓词提取的运行源替代，不解除业务源冻结，不改变九项政策指纹或旧配置。

### 1.17.9 — 2026-09-07

- M12 C1n：固定每日runner及工作流双重禁用；只从已提交配置和受保护Actions上下文构造客户端，业务导入前验证完整源字节，不接受CLI目标。首接点仅成员归档且明确未formal，后续股票池／日链接通前不称完整每日生产；上线仍单独批准。

### 1.17.8 — 2026-09-07

- M12 C1m：每日客户端只能处理与自身固定Actions上下文一致的准备输入，调用唯一固定进程；服务器回执必须匹配实际输入／输出字节，确认后才可进入现有成员字节桥。失败关闭本客户端，不自动重发或自行恢复许可。

### 1.17.7 — 2026-09-07

- M12 C1l：每日准备认证路由只能在服务器固定目标和已初始化epoch下获取／续持daily租约；已核验返回经持久选择与配对日志绑定原任务。成员路由从服务器选择取得能力，正文不能指定任务／策略／租约，响应前复查选择；默认禁用，无生产挂接。

### 1.17.6 — 2026-09-07

- M12 C1j：准备worker须在业务导入前核对完整运行提交与services实际源字节，无外部导入根／额外文件或替换对象；固定计算进程复用取消／清理，限定30秒并核对原输入摘要后回传。该代码绑定不替代平台OIDC或使用前当前根检查。

### 1.17.5 — 2026-09-07

- M12 C1i：准备验证输入须先按原字节归档、与原身份／daily租约／当前根配对记账，固定身份回传只能绑定该原输入；使用前重新读回输入与结果并核对配对记录和现时根／租约。不得将摘要当签名或新增权限，不放宽原身份期限。

### 1.17.4 — 2026-09-07

- M12 C1h：受信准备材料经封闭私有输入进入唯一Python合同检查，当前授权、完整Git配置与原身份代码共同绑定；计算起止仍在原身份窗口，完成时再次核验授权日期。输出绑定精确输入，仍非运行许可，实际使用须重查服务端当前根及供应商许可；无生产路由或启用。

### 1.17.3 — 2026-09-07

- M12 C1g：当前准备材料只能由服务端固定目标在真实OIDC／daily租约下，从当前持久索引读回全部授权原件和配置原件；每次异步读回后与输出前重查完整根及有效期。默认禁用，材料不赋权，不在消费者复制Python合同规则，也不建立生产许可工厂或路由。

### 1.17.2 — 2026-09-07

- 细化M12首轮配置的政策全集、固定Git业务源与明确运行提交；无策略优化、真实批准或生产启用。

### 1.17.1 — 2026-09-07

- 细化M12当前研究准备授权的链头／撤销、配置字节及双日期核验；不启用任何生产权限或改变回退边界。

### 1.17.0 — 2026-09-06

- 记录M12 v0.2.1实施批准、唯一合同入口与跨日责任；保留上线卡及旧生产边界。

### 1.16.0 — 2026-08-30

- 多因子页改为“颗数＋家族＋重复确认＋跨周期”复杂版，行业／大盘不再组成覆盖技术证据的总分。
- 个人形态页改为四项简化版，七阶段只读历史不再占据主交互。

### 1.15.0 — 2026-08-30

- 将仍需复现但不供四个正式页面读取的早期研究JSON迁出`public/`，只保存在Git研究档案。
- 清除孤立模板资产、旧页面专用产物和从未接入生产的原型，缩小网站发布载荷；每日生产数据合同不变。
- 当前通知环境变量统一使用Sage Vista名称，生产网址、通知内容和去重语义不变。

### 1.14.0 — 2026-08-30

- 正式前台从五个入口收敛为今日、多因子机会、我最喜欢形态、行业与大盘四个入口；实验与回测明细退回Git后台。
- 规定共享外壳只读取轻量更新状态，个人形态使用独立精简数据，避免所有页面重复下载旧Tracker。
- 多因子页保留完整决策功能但移除重复实验组合卡；行业页优先显示SPY、QQQ、IWM、RSP与SOXX等常用ETF快读。
- 纯研究文件变更不再触发网站部署；每日EOD、评分、排行、风险、通知和线上日期核验保持不变。

### 1.13.0 — 2026-08-30

- 把案例从“标准图形”改为机制教学，新增接近机会、明确缺项和风险阻断展示。
- PG退回V1历史，V2页面不再把它当当前参考案例；正式信号与近似观察严格分区。

### 1.12.0 — 2026-08-30

- “我最喜欢形态”改用两段确认时间线，新增ADBE正例与TTD／AEVA风险反例，并分开EMA转强与完整排列。
- 继续禁止把匹配度包装成收益概率；V2在达成前向门槛前只显示进度和可复查案例。

### 1.11.0 — 2026-08-29

- 新增独立“我最喜欢形态”功能页，只承载单一形态漏斗、当日候选、图形证据、参考案例和前向样本。
- 明确新页不恢复旧MACD Tracker产品，不复制多因子排行或旧因子库，并禁止把形态匹配度画成胜率。

### 1.10.1 — 2026-08-29

- 组合收益卡固定用并排条形图显示历史滚动留出、已见2025和已见2026的组合与共同门票，不用单一冠军数字隐藏失效时期。
- 本轮历史冠军使用警示态并显示生产权重0、样本、滚动段数和BH q；完整15组合仍只进实验档案。

### 1.10.0 — 2026-08-29

- 登记因子家族组合实验的网站交付：主页面只放精简收益对照，完整15组合继续留档。
- 强制分开历史收益冠军、零权重前向候选和生产规则，防止网站把回溯冠军包装成买入承诺。

### 1.9.0 — 2026-08-29

- 多因子机会页收敛为当前决策链，移除旧系统历史参考、重复旧因子库、独立个股查询、旧评分兼容和旧规则区。
- 同步删除上述模块对应的前端数据请求与状态；历史事实和实验档案仍保留在研究页与GitHub。
- 登记下一阶段可视化边界：回测口径冻结后再加入收益曲线、条形图和构成图，不能用图形夸大未验证因子。

### 1.8.0 — 2026-08-29

- 因子库改为“正在使用 / 候选观察 / 暂停加权 / 准备弃用”四象限，并强制分开当前生产角色与研究有效性。
- 为资格、趋势、MACD、支撑、价格结构、RSI、量能和风险建立稳定家族色；文字仍是权威含义。
- 要求可执行结论与四象限共同读取最近完成的审计实验，旧摘要不得覆盖新结论。

### 1.7.1 — 2026-08-29

- 将原8条 Cloudflare Cron 表达式合并为2条：日终宽窗口加内部精确白名单，以及独立新鲜度检查。
- 保留原7个日终启动时点；额外唤醒只空退出，不 dispatch GitHub 工作流。

### 1.7.0 — 2026-08-29

- 将 Cloudflare Cron 设为每日 EOD 和新鲜度检查的独立主触发源，GitHub schedule 降为备用。
- 新鲜度落后时自动补跑一次日终更新，并记录每次运行的触发来源。
- 明确外部调度必须使用本仓库最小 Actions 权限专用令牌，禁止复用个人广权限凭据。

### 1.5.0 — 2026-08-29

- 明确多周批量回放仍按周独立留档，全部成功后才聚合上线。
- 历史网站数据保留最近30个交易日完整审计，较早日期只省略重复的未命中因子，不删除排名、分数、命中和研究周产物。

### 1.4.0 — 2026-08-28

- 要求夜间回测的启用状态、覆盖范围、下一断点和规则版本直接显示在历史实验页。
- 建立每天一个自然周、成功才推进、失败原周重试和旧批次版本冻结的生产约束。

### 1.3.0 — 2026-08-28

- UI / 实验变更自动部署与 EOD 数据更新拆成两条可复用通道。
- 明确每日使用当前批准生产规则；没有新交易日时保留最新完整收盘，而不是伪造更新。

### 1.2.0 — 2026-08-28

- 要求历史实验页展示每条实验的时间、中文问题、结果、用法和状态，并与 GitHub 中文总结同源。
- 要求行业与大盘合并为实用决策页：大盘结论置顶，行业热点与风险随后，原始表格下沉。

### 1.1.0 — 2026-08-28

- 删除旧个股研究 / MACD Tracker 产品页，将个股解释并入唯一多因子排行榜。
- 规定主导周期标签必须出现在排行榜和个股详情，并标明尚未验证的持仓含义。

### 1.0.0 — 2026-08-28

- 明确网站为最终成品，定义页面职责、实用表达和生产闸门。
